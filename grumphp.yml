grumphp:
    environment:
        files:
            - .env
        paths:
            - vendor/bin

    tasks:
        phpcs:
        phpstan:
            configuration: phpstan.neon
            memory_limit: "-1"
            use_grumphp_paths: false
        phpunit:
            always_execute: true